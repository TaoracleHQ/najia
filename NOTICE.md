# 第三方内容与授权

本仓库包含两类内容，授权不同。

## 代码

`src/`（除 `src/data/`）、`test/`、`tools/` —— **MIT**，见 `LICENSE`。

其中的推导逻辑移植自 Python 库 [`najia`](https://github.com/bopo/najia)（MIT，作者 bopo），
`LICENSE` 中保留了其版权声明。

## 卦辞数据

`src/data/guaci.json` —— 《周易》经文、彖传与象传，转录自
[中文维基文库《周易》](https://zh.wikisource.org/wiki/周易)。

授权情况需要分两层看：

- **《周易》本文属公有领域。** 成书于先秦，早已超出任何著作权保护期限。
- **维基文库的转录**按站点条款以 **CC BY-SA 4.0** 提供。不过，对一份公有领域文本
  的忠实转录是否构成受保护的新作品，在各法域尚有争议（美国法下"额头出汗"不产生
  著作权；欧盟另有数据库权）。

因此本仓库的做法是：**无论上述争议如何认定，都完整署名维基文库。** 若你的使用
场景对 CC BY-SA 的相同方式共享条款敏感（例如需要闭源再分发这份数据），建议：

1. 自行改用一份授权明确的底本替换 `src/data/guaci.json`，或
2. 直接依据公有领域的《周易》原文重新录入

`tools/build-guaci.py` 是可复现的生成脚本，替换底本时改它即可。

## 依赖

`tyme4ts` —— 日期与干支计算，MIT，作者 6tail。
